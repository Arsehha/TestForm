client:

- (добавлено) - новый ui css кит для ускорения разметки

- (убрано) - дублирование css кода

- (fix) - компонент CustomRadio - ошибка консоли: неправильно передавался id

- (fix) - disabled для форм при отправке. Форма отключается полностью
          при отправке запроса, а не только отдельные её части.

- (fix) - изменена логика иконок. Достаточно в path передать только название иконки,
          например mdiDelete в виде строки. Импорт производится на стороне компонента.

- (fix) - слетающие стили кнопки. Стили кнопки были вынесены в assets/main.css